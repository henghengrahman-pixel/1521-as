"use client";
import {useState} from 'react';import {useRouter} from 'next/navigation';import {MapPin,CheckCircle2} from 'lucide-react';
export function CheckoutForm({service,areas,partner}:{service:{id:string;name:string;basePrice:number},areas:{id:string;name:string}[],partner?:{id:string;businessName:string;customPrice:number|null}|null}){const r=useRouter(),[busy,setBusy]=useState(false),[err,setErr]=useState(''),[coords,setCoords]=useState<{lat:number;lng:number}|null>(null);function locate(){if(!navigator.geolocation){setErr('Geolocation tidak didukung browser ini.');return}navigator.geolocation.getCurrentPosition(x=>{setCoords({lat:x.coords.latitude,lng:x.coords.longitude});setErr('')},()=>setErr('Lokasi perangkat tidak dapat diakses. Pilih alamat secara manual.'))}async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setErr('');try{const f=new FormData(e.currentTarget),body={serviceId:service.id,partnerId:partner?.id,areaId:f.get('areaId'),name:f.get('name'),phone:f.get('phone'),address:f.get('address'),scheduledAt:f.get('scheduledAt'),quantity:Number(f.get('quantity')||1),note:f.get('note'),locationNote:f.get('locationNote'),paymentMethod:f.get('paymentMethod'),latitude:coords?.lat,longitude:coords?.lng};const res=await fetch('/api/orders',{method:'POST',headers:{'content-type':'application/json','idempotency-key':crypto.randomUUID()},body:JSON.stringify(body)}),j=await res.json();if(!res.ok){setErr(j.error||'Gagal membuat order');return}r.push(`/pesanan/${j.publicId}`)}catch{setErr('Koneksi bermasalah. Silakan coba lagi.')}finally{setBusy(false)}}return <form onSubmit={submit} className="checkout-v6-card"><div className="checkout-v6-head"><div><h2>Detail Pesanan</h2><p>Harga dihitung ulang di server saat order dibuat.</p>{partner&&<p><b>Mitra pilihan: {partner.businessName}</b></p>}</div><strong>Mulai Rp {service.basePrice.toLocaleString('id-ID')}</strong></div><div className="checkout-v6-grid"><label>Nama<input required name="name" autoComplete="name"/></label><label>No. HP / WhatsApp<input required name="phone" inputMode="tel" autoComplete="tel" placeholder="08xxxxxxxxxx"/></label><label>Area<select required name="areaId"><option value="">Pilih area</option>{areas.map(a=><option key={a.id} value={a.id}>{a.name}</option>)}</select></label><label>Jadwal<input required type="datetime-local" name="scheduledAt"/></label><label className="full">Alamat lengkap<textarea required name="address" rows={3} autoComplete="street-address"/></label><label>Patokan / catatan lokasi<input name="locationNote"/></label><label>Jumlah<input required min="1" max="20" defaultValue="1" type="number" name="quantity"/></label><label>Metode pembayaran<select name="paymentMethod"><option value="CASH">Bayar setelah layanan / Cash</option><option value="TRANSFER">Transfer</option></select></label><label className="full">Catatan<textarea name="note" rows={2}/></label></div><button type="button" className="btn outline checkout-v6-location" onClick={locate}>{coords?<><CheckCircle2 size={17}/> Lokasi perangkat tersimpan</>:<><MapPin size={17}/> Gunakan Lokasi Perangkat</>}</button>{coords&&<small className="muted">Lokasi perangkat akan divalidasi di server untuk matching; koordinat tidak ditampilkan di storefront.</small>}{err&&<p role="alert" className="checkout-v6-error">{err}</p>}<button disabled={busy} className="btn primary checkout-v6-submit">{busy?'Memproses...':'Konfirmasi Order'}</button></form>}
